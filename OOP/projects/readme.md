Write a java program that stores and displays info about students
Create a class called person, with the attributes of name and age
1. Create a class called students that inherits person, the class should contain the following variables:
int score
2. Create the following methods, 
displayInfo(), it should display name, age and score
checkResult(), it should display pass if score >= 50, else display Fail
3. in the main class, create an array that can store 3 students
4. use a loop to:
collect student detail, create student objects, and store them in the array
collect: name, age, score
5. Use another loop to display all student information and to display either they passed or fail.